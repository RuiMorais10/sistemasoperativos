#ifndef ITOA_SAFE_H
#define ITOA_SAFE_H

/**
 * @brief Converts an int to a string
 * 
 * @param string pointer to a pre-allocated string
 * @param number 
 * @return int 
 */
int itoas(char* string, int number);

#endif